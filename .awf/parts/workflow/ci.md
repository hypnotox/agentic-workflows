Local hooks are optional per-clone preflight. `.github/workflows/ci.yml` always runs complete Linux Go behavior for pull requests, consumes one typed JSON v2 selection, and runs complete native Linux/amd64 and Darwin/arm64 assurance on `main`. The closed CI lane inventory is `go`, `platform-sensitive`, `release-archive`, and `render-template`. One aggregate `CI / gate` result is the definitive hosted verdict. Inspect it before treating a pushed commit as verified.