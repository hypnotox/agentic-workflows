Pursue these outcomes in order:

1. Publish the standard as a versioned, discoverable artifact with an examples gallery.
2. Audit this repository's overrides and retain only the project-specific differences from template defaults.
3. Replace prose inventories of bounded code-design candidates with a static-state inventory command. Keep the retired `awf doctor` name unavailable unless a distinct meaning is established.

### v0.46 adopter rollout

Release preparation must first prove schema 50, command behavior, and retained generated product bytes against the complete native matrix. Do not create schema 51, mutate release history, or change adopter repositories as part of the architecture milestone.

After a separately approved v0.46 release, update the nine managed adopters from their committed v0.45/schema-50 tips: `jugend-im-zentrum`, `nouris`, `fleet`, `pi-cockpit`, `pi-tools`, `aeonseed`, `pi-science`, `sudoku-solver`, and `razer-kb-manager`. The expected adopter delta is only the pinned awf version and same-schema synchronization metadata. R0-R5 change internal package ownership and repository authority, not adopter configuration or retained generated artifacts. For each adopter, run the supported upgrade/render path, require deterministic `awf check` plus native repository gates, compare generated bytes and pruned paths with the v0.45 baseline, and stop on any unexplained product-byte, schema, command, or runtime change rather than absorbing it into rollout.

Keep repositories disjoint, one owner per adopter, and record before/after awf versions, schema, default-branch commit, exact rollout commit, generated-byte result, checks, and unrelated failures. Close only after all nine default branches are committed and a fleet census confirms no new configuration key, compatibility shim, retired Pi effort output, or local workaround was needed. GitHub evidence cannot prove the absence of ignored journals, stale local worktrees, or already-running Pi processes, so the closure report must retain that limitation.
