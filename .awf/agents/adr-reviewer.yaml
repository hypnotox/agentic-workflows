data:
    focusItems:
        - description: ADRs that change the config-tree schema or the lock format must enumerate all consumers (config, render, manifest, migrate) and confirm backward compatibility or a migration path.
          name: Schema stability
        - description: ADRs touching templates must confirm the missingkey=zero constraint is preserved; no template may render a no-value token when vars are empty strings.
          name: Template invariants
        - name: dependency-composition-authority
          description: when the ADR changes dependency selection, ownership, or wiring, consult code-design/dependency-composition and flag a speculative capability or a capability without one concrete first consumer
        - name: state-ownership-authority
          description: when the ADR changes what a value owns, where derived state lives, or the lifetime of a cache, consult code-design/state-ownership and flag a field written after construction, a derivation stored on a value that outlives its operation, and any correctness that rests on a caller remembering to reset, reload, or order a call
        - name: single-home-authority
          description: when the ADR introduces or converts a shared policy or mechanism, consult code-design/single-home and flag a second implementation of a concern another package already owns, a reduced copy where configuring the shared one would serve, and any fork justified by coverage rather than by a materially different contract from a distinct source
        - name: presentation-ownership-authority
          description: when the ADR changes where a result model is rendered for humans, consult code-design/presentation-ownership and flag a design that renders a model outside the package owning it or passes a model across a package boundary as a loosely-typed map
        - name: outcome-modeling-authority
          description: when the ADR changes how an outcome is surfaced, matched, or asserted, consult code-design/outcome-modeling and flag a design that branches on message substrings, reaches for a shallow os.Is* predicate, introduces an error identity without a branching consumer, or shapes a repository-state refusal outside the actionable outcome protocol
        - name: package-composition-authority
          description: when the ADR adds a package or exported surface, consult code-design/package-composition and flag a package without a one-sentence ownership statement, a grab-bag home, an export without an outside-package production consumer, and an undocumented exported declaration
        - name: test-design-authority
          description: when the ADR shapes test strategy or test seams, consult code-design/test-design and flag a design that assumes an assertion library or mints a new package-level variable existing to be swapped by tests
        - name: rationale-quality-at-each-operation
          description: "the Decision item behind each add, update, or remove must explain why that specific claim is created, changed, or retired; structural checks prove only that an operation is valid, not that it is warranted"
        - name: claim-agrees-with-its-own-decision
          description: 'read EACH claim this ADR adds or updates against the Decision items of THIS SAME ADR, and flag any clause that contradicts one. No mechanical layer catches this: backing validation is structural, so it confirms a proof marker exists and never compares claim prose to the Decision or to what the proof asserts, and a decision that disagrees with itself leaves every layer mutually consistent and the gate green. ADR-0183 shipped exactly this defect and it survived two schema generations: item 1 committed to "topic coverage and topic fan-out always evaluate" while the same ADR''s `config/configuration:severity-not-configurable` claim ended "a tree that declares no currentState block requests neither", and a test pinned the claim''s side, so the implementation followed the claim and nothing ever went red. ADR-0192 had to correct it. The asymmetry to watch for is a claim clause that quietly scopes, qualifies, or excepts something the Decision states without qualification, and an aside appended to a claim about a different subject is where it hides.'
        - name: claim-topic-cohesion
          description: 'each claim this ADR adds belongs in the topic its State changes names: it answers the same question that topic''s existing claims answer, rather than landing there because the topic is adjacent or convenient. Flag a destination that gives its topic a second subject, and name the subject the claim belongs to instead. Judge by subject, never by how many claims the topic already holds.'
