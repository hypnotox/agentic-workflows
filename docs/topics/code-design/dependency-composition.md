<!-- GENERATED by awf: do not edit; change .awf/ and run `awf render` -->
# Dependency composition

How awf selects, owns, and wires volatile dependencies without speculative abstraction.

**Applicability:** Global topic within owning domain selectors none. Run `awf topic code-design/dependency-composition --coverage` for current matched paths and marker sites.

This topic owns awf's repository-wide dependency-composition rules. Claims are added through ADR-0178 when its working foundation is implemented; until then the Proposed ADR is rationale, not active authority.

## Claims
