<!-- GENERATED by awf: do not edit; change .awf/ and run `awf sync` -->
# Adapter outputs

Generated executable adapter-runtime outputs and their ownership boundary.

**Applicability:** Owning domain selectors: `.pi/extensions/**`, `internal/catalog/**`, `internal/project/**`, `internal/refs/**`, `internal/render/**`, `templates/**`. Topic selectors: `.pi/extensions/**`. Both domain and topic selectors must match. Run `awf topic rendering/adapter-outputs --coverage` for current matched paths and marker sites.

This topic records the current ownership contract for generated executable adapter-runtime outputs.

## Claims

### `invariant: generated-adapter-runtime-ownership`

Enabled target extension outputs under `.pi/extensions/**` are owned by this topic even though their generated-output classification excludes them from whole-tree coverage eligibility.
Origin: ADR-0144
Backing: test

### `invariant: pi-workflow-dashboard-runtime`

The generated awf-dashboard runtime restores only active-branch association, validates and appends protocol 2 through confined owner-only leased storage, drains serialized writes on shutdown, and keeps passive failures non-blocking while explicit lifecycle failures remain visible. A fresh session buffers at most 256 privacy-filtered observations and 1 MiB of canonical UTF-8 bytes in memory until deterministic creation or explicit resume; overflow settles before direct append, graceful boundaries retry settlement, and a pre-commit crash can lose only that bounded window. The muted below-editor widget synchronously applies validated local badge state and computes footer-parity totals once from unique active-branch assistant entries, including restored and top-level nested-subagent usage, while public context usage supplies only current context. Canonical refresh assigns generations before launch, coalesces controlled boundaries, applies metrics then doctor only when not superseded by newer local or canonical state, and notifies an open overlay. Findings are re-resolved under their `effortId`, evidence, scope, eligibility, and current nonempty frontier before repair or waiver. These surfaces never infer workflow state, double-charge tool-result or compaction usage, import private footer state, or persist conversational content, raw tool arguments, commands, output, or unrestricted paths. Canonical reads use one session-captured bootstrap, PATH, or advertised pinned launcher; launcher calls carry the absolute project root in `AWF_DASHBOARD_PROJECT_ROOT`, while mutation and maintenance continue through ordinary gated binaries and never through the private read-only fallback.
Origin: ADR-0146
Revised-by: ADR-0150, ADR-0149
Backing: test
