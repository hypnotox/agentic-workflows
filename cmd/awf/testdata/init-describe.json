{
  "descriptors": [
    {
      "key": "profile",
      "kind": "enum",
      "description": "Governance footprint: Core includes the operational workflow; Full adds ADR, plan, current-state authority, and audit capabilities at the same quality bar.",
      "default": "core",
      "options": [
        "core",
        "full"
      ],
      "target": "profile"
    },
    {
      "key": "gateCmd",
      "kind": "string",
      "description": "Command that runs the fast pre-commit gate.",
      "default": "",
      "options": [
        "make gate",
        "go test ./..."
      ],
      "target": "var"
    },
    {
      "key": "gateCmdFull",
      "kind": "string",
      "description": "Command for terminal exhaustive verification, if the project has one.",
      "default": "",
      "options": [
        "make gate-full"
      ],
      "target": "var"
    },
    {
      "key": "checkCmd",
      "kind": "string",
      "description": "Command that checks rendered output for drift. Leave empty to run through the always-rendered `./awf` wrapper.",
      "default": "",
      "options": [
        "./awf check",
        "make check"
      ],
      "target": "var"
    },
    {
      "key": "commitGateCmd",
      "kind": "string",
      "description": "Command that validates one commit message (the commit-msg hook payload appends the message-file argument). Leave empty to run through the always-rendered `./awf` wrapper.",
      "default": "",
      "options": [
        "./awf check staged commit"
      ],
      "target": "var"
    },
    {
      "key": "testCmd",
      "kind": "string",
      "description": "Command that runs the test suite.",
      "default": "",
      "options": [
        "go test ./...",
        "npm test",
        "make test"
      ],
      "target": "var"
    },
    {
      "key": "commitScopes",
      "kind": "string",
      "description": "Comma-separated Conventional Commits scopes this project allows. Written to audit.allowedScopes and enforced by awf check staged commit/audit and quoted by the agent guide. Leave empty to accept any scope.",
      "default": "",
      "options": [
        "adr,awf,plans"
      ],
      "target": "audit-scopes"
    },
    {
      "key": "activeMdRegenCmd",
      "kind": "string",
      "description": "Command that regenerates the generated ADR decision index (INDEX.md).",
      "default": "",
      "options": [
        "./awf render",
        "awf render"
      ],
      "target": "var"
    },
    {
      "key": "invariantTestPath",
      "kind": "string",
      "description": "Path or glob where invariant-backing tests live.",
      "default": "",
      "options": [
        "./internal/..."
      ],
      "target": "var"
    }
  ]
}
