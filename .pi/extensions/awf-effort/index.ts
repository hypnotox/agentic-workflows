// GENERATED by awf: do not edit; change .awf/ and run `awf render`
// @ts-nocheck
import { randomUUID } from "node:crypto";
import { join } from "node:path";
import { stat } from "node:fs/promises";
import { Type } from "typebox";
import { activity, type ActivityExecutor, type ActivityReply } from "./client.ts";
const SLUG=/^[a-z0-9]+(?:-[a-z0-9]+)*$/;
type Snapshot=Readonly<{slug:string;owner:string;title:string;memory:Readonly<{phase:string;next:string;updated:string}>|null;heartbeatAt:string;managedWorktree:boolean}>;
type Context={cwd:string};
export type RemotePiMetadata=Readonly<{effort:Readonly<{slug:string;title:string}>;memory:Snapshot["memory"];activity:Readonly<{heartbeatAt:string}>}>;
export type RemotePiMetadataSetPayload=Readonly<{namespace:"awf";value:RemotePiMetadata|null}>;
export type RemotePiCapabilitiesReplyPayload=Readonly<{metadata?:Readonly<{version?:unknown}>;nameOverride?:Readonly<{version?:unknown;namespaces?:unknown}>}>;
type PiLike={exec:ActivityExecutor;registerTool(t:unknown):void;on?(name:string,handler:(event:any,ctx:Context)=>any):void;events?:{emit(name:string,payload?:unknown):unknown;on?(name:string,handler:(payload:any,ctx?:Context)=>void):unknown}};
export type EffortExtensionDependencies=Readonly<{uuid?:()=>string;isDirectory?:(path:string)=>Promise<boolean>}>;
function immutable(r:ActivityReply,owner:string,present:boolean):Snapshot|undefined{if(!r.effort||!r.memory||!r.activity||r.activity.owner!==owner)return;return Object.freeze({slug:r.effort.slug,owner,title:r.effort.title,memory:Object.freeze({phase:r.memory.phase,next:r.memory.next,updated:r.memory.updated}),heartbeatAt:r.activity.heartbeatAt,managedWorktree:present})}
function render(reply:ActivityReply):string{const o=reply.outcome!;const actions=o.nextActions.length===1?o.nextActions[0]:o.nextActions.map((a,i)=>`${i+1}. ${a}`).join(" ");return `${o.category}; ${o.condition}; changedActivity=${o.changedActivity}${o.cause?`; cause=${o.cause}`:""}; ${actions}`}
export function registerEffort(pi:PiLike,deps:EffortExtensionDependencies={}):void{const uuid=deps.uuid??randomUUID, owner=uuid(), directory=deps.isDirectory??(async path=>{try{return (await stat(path)).isDirectory()}catch{return false}});if(!/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/.test(owner))throw new Error("extension owner must be a lowercase UUIDv4");let current:Snapshot|undefined,nameOK=false,namePublished=false,chain=Promise.resolve();const emit=(name:string,payload?:unknown)=>{try{pi.events?.emit(name,payload)}catch{}};const clearName=()=>{if(namePublished){emit("remote-pi:name-override:set",{namespace:"awf",value:null});namePublished=false}};const publish=()=>{const x=current;if(!x){emit("remote-pi:metadata:set",{namespace:"awf",value:null});clearName();return}emit("remote-pi:metadata:set",{namespace:"awf",value:{effort:{slug:x.slug,title:x.title},memory:x.memory,activity:{heartbeatAt:x.heartbeatAt}}});if(nameOK){emit("remote-pi:name-override:set",{namespace:"awf",value:x.slug});namePublished=true}else clearName()};const serial=<T>(f:()=>Promise<T>)=>{const run=chain.then(f,f);chain=run.then(()=>undefined,()=>undefined);return run};const invoke=(ctx:Context,op:"attach"|"heartbeat"|"detach",slug:string,owner:string,signal?:AbortSignal)=>activity(pi.exec,ctx.cwd,op,slug,owner,signal);const detach=async(ctx:Context,signal?:AbortSignal)=>{if(!current){publish();return "Detached."}const before=current;const r=await invoke(ctx,"detach",before.slug,before.owner,signal);if(r.condition==="detached"||r.condition==="missing"||r.condition==="not-owner"){current=undefined;publish();return "Detached."}return render(r)};pi.registerTool({name:"using_effort",label:"Use effort",description:"Explicitly attach or detach this Pi session from an effort without changing CWD.",parameters:Type.Object({effort:Type.Optional(Type.String({minLength:1,maxLength:255})),detach:Type.Optional(Type.Boolean())},{additionalProperties:false}),execute:async(_id:string,args:{effort?:string;detach?:boolean},signal:AbortSignal,onUpdate:unknown,ctx:Context)=>serial(async()=>{void onUpdate;if(!Object.keys(args).every(key=>key==="effort"||key==="detach")||(args.detach===true)===(args.effort!==undefined))throw new Error("supply exactly one of effort or detach");if(args.detach===true)return {content:[{type:"text",text:await detach(ctx,signal)}]};if(!SLUG.test(args.effort!))throw new Error("effort must be a canonical slug");if(current&&current.slug!==args.effort){const result=await detach(ctx,signal);if(current)return {content:[{type:"text",text:result}]}}const r=await invoke(ctx,"attach",args.effort!,owner,signal);if(r.condition!=="attached"&&r.condition!=="taken-over")return {content:[{type:"text",text:render(r)}]};const present=await directory(join(ctx.cwd,".awf","worktrees",args.effort!)).catch(()=>false);const next=immutable(r,owner,present);if(!next)throw new Error("awf returned an incomplete attached reply");current=next;publish();return {content:[{type:"text",text:`Attached to ${next.slug}.`}]}})});pi.on?.("context",(event:any)=>{if(!current)return;const x=current;return {messages:[...event.messages,{role:"custom",customType:"awf-effort",content:`[awf effort] active=${x.slug} memory=.awf/efforts/${x.slug}/memory.md${x.managedWorktree?` managedWorktree=.awf/worktrees/${x.slug}`:""}`,display:false,timestamp:Date.now()}]}});pi.on?.("turn_end",(_event:any,ctx:Context)=>serial(async()=>{if(!current)return;const before=current;try{const r=await invoke(ctx,"heartbeat",before.slug,before.owner);if(r.condition==="not-owner"||r.condition==="missing"){current=undefined;publish();return}if(r.condition!=="heartbeat"){current=Object.freeze({...before,memory:null,managedWorktree:false});publish();return}const present=await directory(join(ctx.cwd,".awf","worktrees",before.slug)).catch(()=>false);current=immutable(r,before.owner,present)??Object.freeze({...before,memory:null,managedWorktree:false});publish()}catch{current=Object.freeze({...before,memory:null,managedWorktree:false});publish()}}));pi.on?.("session_start",(_event:any)=>{current=undefined;publish()});pi.on?.("session_shutdown",(_event:any,ctx:Context)=>serial(async()=>{try{await detach(ctx)}catch{}current=undefined;publish()}));pi.events?.on?.("remote-pi:metadata:request",()=>publish());pi.events?.on?.("remote-pi:name-override:request",()=>publish());pi.events?.on?.("remote-pi:capabilities",(caps:RemotePiCapabilitiesReplyPayload)=>{nameOK=caps?.metadata?.version===1&&caps.nameOverride?.version===1&&Array.isArray(caps.nameOverride.namespaces)&&caps.nameOverride.namespaces.includes("awf");publish()});emit("remote-pi:capabilities:request")}
export default function effortExtension(pi:PiLike):void{registerEffort(pi)}
