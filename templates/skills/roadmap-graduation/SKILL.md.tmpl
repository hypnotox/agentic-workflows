---
name: {{ .prefix }}-roadmap-graduation
description: Use when a {{ .layout.docs.roadmap }} entry graduates to an ADR or a PR, or is explicitly dropped. Removes the entry in the same commit; re-verifies any cited measurements before migrating them.
---

# {{ .prefix }}-roadmap-graduation

A support skill for moving an item out of `{{ .layout.docs.roadmap }}` into an ADR or PR, or dropping it, without carrying stale measurements or leaving a stale entry.

<!-- awf:section when-fires -->
## When this skill fires

Use when a roadmap item becomes an ADR or PR, or is explicitly dropped. This support skill sits outside the workflow chain.
<!-- awf:end -->

<!-- awf:section failure-modes stub -->
<!-- awf:end -->

## Procedure

Graduation may remain effort-free. Identify and re-verify the roadmap item before deciding whether continuity materially helps; neither reaching this procedure nor citing an item creates an effort. An existing effort resumes under its fixed identity only while graduation remains within its outcome. Confirm either legacy `Effort: <slug>` or canonical frontmatter `effort: <slug>` identity when ownership exists. Repository sources and current-state documentation outrank checkpoint prose, children receive any parent slug and exact `.awf/efforts/<slug>/memory.md` path read-only and never edit memory, standalone memory is forbidden, and one user-managed writer remains responsible.

<!-- awf:section identify-entry -->
### 1. Identify the roadmap entry
Find its block under the right section of `{{ .layout.docs.roadmap }}`. Read it: does it cite measured numbers (benchmarks, query timings, payload sizes)?
<!-- awf:end -->

<!-- awf:section reverify-measurements -->
### 2. Re-verify cited measurements
If the entry cites inline numbers, re-measure on the current code before graduating. Roadmap entries can be old; the numbers may not be honest anymore. If the new measurement differs materially, use the **current** number in the graduating artifact (ADR Context or commit body). The old roadmap number is removed or, if both are useful, cited as "originally X; current Y".
<!-- awf:end -->

<!-- awf:section graduate-single-commit -->
### 3. Evaluate first effort creation

Evaluate continuity independently after identifying and re-verifying the item. Invoke `{{ if index .skills "effort-workflow" }}{{ .prefix }}-effort-workflow{{ else }}the effort lifecycle owner{{ end }}` before ADR or implementation mutation only when durable continuity materially helps; otherwise graduation remains effort-free.


### 4. Graduate
- **Architectural -> ADR:** {{ if index .skills "proposing-adr" }}Invoke `{{ .prefix }}-proposing-adr`.{{ else }}Write the ADR per the project's decision process.{{ end }}
- **Small -> PR:** Implement it and capture the roadmap reasoning in the commit body.

Remove the entry in the graduating commit, or the final implementation commit when an ADR spans commits.
<!-- awf:end -->

<!-- awf:section explicit-drop -->
### 5. Explicit drop
Commit with subject `docs(roadmap): drop <item>`; the one-line reason goes in the commit body, not the subject or the file.
<!-- awf:end -->

<!-- awf:section same-commit -->
Never leave a graduated or dropped entry parked for later cleanup.
<!-- awf:end -->

<!-- awf:section doc-currency -->
### 6. Doc-currency
If the graduated entry mentions a state doc or ADR that needs updating, fold that update into the same commit.
<!-- awf:end -->

<!-- awf:section notes -->
## Notes

- Does not commit on your behalf.
- `{{ .layout.docs.roadmap }}` owns the backlog; this skill owns graduation procedure.
- When a roadmap entry's removal lands in the same commit as an ADR, the commit's Conventional Commits scope follows the ADR's domain (e.g. `docs(adr)`), not `(roadmap)`. The roadmap delta is a doc-currency obligation of the ADR, not a separate concern.
<!-- awf:end -->
